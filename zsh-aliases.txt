/* custom_alias */

# Alias for ls.
alias la='ls -a'
alias li='ls -li'
alias lh='ls -lhA'

# Other Alias.
alias gp='grep -i'
alias his='history -c'
alias ac='acpi -b'
alias cat='bat -n'
alias ec='echo'
doas tee /sys/class/backlight/amdgpu_bl0/brightness <<< 60

nerdfetch

figlet ' Sveiki!! '

toilet -t --metal Visada, OH-MY-BASH !

toilet -t --metal Nuostabus simpatija ...

toilet -t --metal Ilga istorija laukia tavęs..!

typeset -g POWERLEVEL9K_INSTANT_PROMPT=off
